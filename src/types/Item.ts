export default interface Item {
  name: string;
  description: string;
  code: string;
  width: number;
  length: number;
  thickness: string;
  area: number;
  category: string;
  hub: string;
  stock: number;
}

// import Item from "../types/Item";

// export default function (array: Item[]): void {
//   const uniqueItems = new Set(
//     array.map((item) => {
//       JSON.stringify(item.code as string);
//     })
//   );

//   const registrosUnicos = Array.from(uniqueItems).map((json) =>
//     JSON.parse(json)
//   );
// }
